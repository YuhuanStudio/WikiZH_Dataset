# 貢獻指南

感謝你對 WikiZH_Dataset 的興趣！我們歡迎所有形式的貢獻。

## 如何貢獻

### 報告錯誤

如果你發現了錯誤，請透過 [GitHub Issues](../../issues) 回報。在提交 Issue 之前，請先搜尋現有的 Issue 以避免重複。

### 建議新功能

歡迎提出新功能的建議。請詳細描述你想要的功能以及使用場景。

### 提交程式碼

1. **Fork 這個專案**
2. **建立功能分支**：`git checkout -b feature/your-feature-name`
3. **進行變更**：
   - 確保程式碼符合現有風格
   - 新增或更新相關測試
   - 更新文件
4. **提交變更**：`git commit -m 'Add some feature'`
5. **推送到你的 Fork**：`git push origin feature/your-feature-name`
6. **提交 Pull Request**

## 程式碼規範

### Python

- 遵循 PEP 8 風格指南
- 使用有意義的變數和函數名稱
- 新增適當的文件字串（docstrings）
- 保持函數簡短並專注於單一責任

### 文件

- 使用 Markdown 格式
- 保持描述簡潔明確
- 包含使用範例

## 開發環境設定

```bash
# 複製專案
git clone https://github.com/YuhuanStudio/WikiZH_Dataset.git
cd WikiZH_Dataset

# 建立環境並安裝相依套件
uv venv --python 3.12 .venv
uv pip install --python .venv/bin/python -r requirements.txt
```

## 修改解析邏輯時

`wiki_parser.py`、`wiki_text.py`、`md_to_dataset.py` 決定了資料集的內容。
改動它們請附上「在真實資料上的前後比對」，而不是只有單元測試：

1. 用同一份 dump 解析數萬篇條目，比對修改前後的**每篇字數增減**
2. 逐篇看變短的條目刪掉了什麼——刪掉標記是對的，刪掉句子不是
3. 確認變長的條目多出來的是內容，不是模板參數或樣式屬性

### 四條最容易踩的線

**① 不要用長度或關鍵字猜「這段是不是雜訊」。**
這種規則在邊界上一定會誤判，而且兩個語言版本的字數不同，同一篇條目會出現
繁體被刪、簡體留著的情形（`公交车` 21 字保留、`公車` 20 字被丟）。請改用
結構判斷：標記配對、行首符號、版面位置。**刪除的理由只能是「沒有內容」，
不能是「太短」或「措辭像雜訊」。**

**② 不要手寫名單，去 dump 裡挖。**
名單永遠補不完，也說不出「補到哪裡算完整」。維基自己的渲染規則就寫在模板頁裡：
資訊框的欄位名在 `| label9 = 出生` 旁邊、維護模板的特徵是引用訊息方塊家族。
掃一次 Template 命名空間就有答案，而且下個月自動跟上維基的變化。

**③ 配對規則要先問「有沒有自閉合的寫法」。**
`<ref name="x" />`、`<nowiki/>`、`<br/>` 都是自閉合的。把它們當成開頭標籤去找
收尾，會配到遠處另一個標籤，中間整段正文被吃掉——實測 `捷爾諾波爾州` 的
簡體版三個章節因此全部消失，而繁體版正常。這種錯誤**單看一個語言版本看不出來**，
是繁簡對等檢查抓到的。

**④ 所有跨度掃描都要設上限。**
括號配不到對時若無條件吃到結尾，一篇條目可能整個消失（實測《聖雄甘地》
少了 33,000 字）。新增的正則還要能通過回溯壓力測試：巢狀量詞
（`(?:key=value\s*)+\|` 這種）在沒有結尾的輸入上會指數級回溯，實測讓全量
解析卡死在單一條目上超過 20 分鐘。

### 送出前一定要跑

```bash
.venv/bin/python qa/cases.py     # 行為黃金案例
.venv/bin/python qa/stress.py    # 回溯壓力測試
```

`qa/cases.py`、`qa/stress.py` 與核心全量閘門都在版控裡；大型驗證資料、稽核日誌與
一次性分析探針仍由 `.gitignore` 排除。若改動需要未收錄的全量探針，請在 PR 說明裡
寫清楚你**怎麼驗證**這個改動沒有弄丟內容。

黃金案例的規矩是：**每修好一個缺陷就留一個案例，註解寫明是哪一篇條目壞過**。
這不是形式——曾經有一次所有殘留字元檢查都是綠的，但程式碼被壓成一行，
是人工翻輸出才發現的，那次之後才有這套案例。

### 留意「逐字區塊」

程式碼（``` 圍欄）、公式（`$…$`）、行內反引號裡的內容是逐字保留的，
不套用散文規則、也不做繁簡轉換（與 MediaWiki 的 LanguageConverter 一致）。
新增清理規則時請確認它不會伸進這些區段——`[[nodiscard]]` 是 C++ 屬性、
`{{1,2,3}}` 是集合論記號、`1-{{2-甲基…}偶氮}` 是化學命名法，這些都不是殘留標記。

## 提交訊息格式

使用清晰的提交訊息格式：

- `feat: 新增新功能`
- `fix: 修復錯誤`
- `docs: 更新文件`
- `style: 程式碼格式調整`
- `refactor: 程式碼重構`
- `test: 新增或更新測試`
- `chore: 其他變更`

## 程式碼審查

所有的 Pull Request 都需要經過審查。審查者可能會要求變更，請保持耐心並積極回應。

## 行為準則

- 尊重所有貢獻者
- 建設性地提供回饋
- 保持對話專業和友善

## 授權

提交你的貢獻代表你同意你的貢獻將使用與本專案相同的授權（Apache 2.0）。

有任何問題嗎？歡迎在 Issue 中提問！
